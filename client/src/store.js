// configure slices (authSlice, coursesSlice, etc.) and export the store.


// 6. Create starter pages & routes

// Client routes (React Router):

// /login & /register

// /courses (catalog)

// /courses/:id (course detail)

// /profile (student dashboard)

// /instructor (instructor dashboard)

// /admin (admin panel)

// Server routes (Express):

// POST /api/auth/register, /api/auth/login

// GET/POST/PUT/DELETE /api/courses (protected by roles)

// GET /api/users/me

// Use axios in your React app to hit these endpoints, dispatch Redux actions, and store JWTs in Redux state (or an HttpOnly cookie).
